[pytest]
# Benchmark-specific pytest config.
# Overrides the root pyproject.toml addopts (which includes --cov-fail-under=85)
# so that running `pytest benchmarks/` directly works without --no-cov.
# The CI workflow always passes --no-cov explicitly; this config protects local runs.
addopts = -v --tb=short
